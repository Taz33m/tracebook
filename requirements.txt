# Keep dependency bounds in the two pyproject.toml files. This convenience
# entry point installs the root implementation owner first and then the
# package-less simulator compatibility facade.
-e .[dev]
-e ./packaging/tracebook-sim[dashboard,analysis,capture]
